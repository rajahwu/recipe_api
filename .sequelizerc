const path = require("path");

module.exports = {
  config: path.resolve("config", "database.js"),
  "models-path": path.resolve("model/db", "models"),
  "seeders-path": path.resolve("model/db", "seeders"),
  "migrations-path": path.resolve("model/db", "migrations"),
};